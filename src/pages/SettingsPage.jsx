import { ExternalLink, Info, LogOut, Moon, Sun } from 'lucide-react';
import toast from 'react-hot-toast';
import { useNavigate } from 'react-router-dom';
import { Button, Card, PageHeader } from '../components/common';
import { useAuth } from '../hooks/useAuth';
import { useTheme } from '../hooks/useTheme';
import { getErrorMessage } from '../utils/helpers';

export default function SettingsPage() { const { theme, setTheme } = useTheme(); const { logout } = useAuth(); const navigate = useNavigate(); const signOut = async () => { try { await logout(); toast.success('You have been signed out.'); navigate('/login'); } catch (error) { toast.error(getErrorMessage(error)); } }; return <div className="mx-auto max-w-4xl space-y-7 animate-soft-in"><PageHeader eyebrow="Settings" title="Make MaintainIQ yours." description="A few focused options for your account and workspace experience." /><Card className="p-5 sm:p-6"><h2 className="font-bold text-ink dark:text-white">Appearance</h2><p className="mt-1 text-sm text-muted">Choose the color mode that feels best for your workspace.</p><div className="mt-5 grid gap-3 sm:grid-cols-2"><button onClick={() => setTheme('light')} className={`flex items-center gap-3 rounded-xl border p-4 text-left transition ${theme === 'light' ? 'border-brand-500 bg-brand-50 dark:bg-brand-950/30' : 'border-line hover:border-brand-300 dark:border-slate-700'}`}><span className="grid h-10 w-10 place-items-center rounded-xl bg-white text-amber-500 shadow-sm"><Sun size={19} /></span><span><span className="block font-semibold text-ink dark:text-white">Light mode</span><span className="mt-1 block text-xs text-muted">Bright and focused</span></span></button><button onClick={() => setTheme('dark')} className={`flex items-center gap-3 rounded-xl border p-4 text-left transition ${theme === 'dark' ? 'border-brand-500 bg-brand-50 dark:bg-brand-950/30' : 'border-line hover:border-brand-300 dark:border-slate-700'}`}><span className="grid h-10 w-10 place-items-center rounded-xl bg-slate-900 text-blue-200"><Moon size={19} /></span><span><span className="block font-semibold text-ink dark:text-white">Dark mode</span><span className="mt-1 block text-xs text-muted">Easy on the eyes</span></span></button></div></Card><Card className="p-5 sm:p-6"><h2 className="font-bold text-ink dark:text-white">Application</h2><div className="mt-5 divide-y divide-line dark:divide-slate-800"><div className="flex items-start gap-3 py-4 first:pt-0"><Info className="mt-0.5 text-brand-600" size={19} /><div><p className="font-semibold text-ink dark:text-white">MaintainIQ 1.0.0</p><p className="mt-1 text-sm leading-6 text-muted">QR-first asset management for a calmer maintenance workflow.</p></div></div><div className="flex items-start gap-3 py-4"><ExternalLink className="mt-0.5 text-brand-600" size={19} /><div><p className="font-semibold text-ink dark:text-white">What’s next</p><p className="mt-1 text-sm leading-6 text-muted">Team collaboration, recurring schedules, and exportable reports are planned improvements.</p></div></div></div></Card><Card className="border-red-100 p-5 dark:border-red-950 sm:p-6"><h2 className="font-bold text-ink dark:text-white">Account session</h2><p className="mt-1 text-sm text-muted">Sign out securely from this device.</p><Button variant="danger" className="mt-5" onClick={signOut}><LogOut size={16} />Log out</Button></Card></div>; }
